{% extends "layout.jinja2" %}

{% block body %}

{# The stuff below should probably go into layout.jinja2, once it has a working hamburger menu. These options should probably only pop up once the user is authenticated. (AKA, "if request.authenticated_userid.") #}
{% if request.authenticated_userid %}
<a href="{{ request.route_url('detail') }}">View Stock Analytics</a>
<a href="{{ request.route_url('portfolio') }}">View Portfolio</a>
<a href="{{ request.route_url('logout') }}">Logout</a>
{% endif %}

{% if error %}
    <p style="color: red">{{ error }}</p>
{% endif %}

<div class="landing-form">
    <a href={{ request.route_url('detail') }}><button class="get-started" type="button">Get Started</button></a>
    <a href={{ request.route_url('login') }}><button class="login-button" type="button">Login</button></a>
    <a href={{ request.route_url('register') }}><button class="sign-up-buttom" type="button">Sign Up</button></a>
<div>

{% endblock body %}
